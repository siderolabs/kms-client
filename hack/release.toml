# commit to be tagged for the new release
commit = "HEAD"

project_name = "kms-client"
github_repo = "siderolabs/kms-client"
match_deps = "^github.com/(siderolabs/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
